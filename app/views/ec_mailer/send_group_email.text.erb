<%= t(:ec_email_head_group) %>
<%= t(:ec_email_period) + " " + @period %>

<% @members.each do |user| %>
    <% totalhours = user.TotalWeekHours %>
    **<%= user.firstname + " " + user.lastname %>
    -- <%= t(:field_weekly_hours) %>: <%= user.weekly_hours %> h
    --  <%= t(:user_weekly_hours) %>: <%= totalhours %> h
    <% if user.weekly_hours >= totalhours %>
        -- <%= t(:ec_hours_left) %>: <%= user.weekly_hours - totalhours %> h

    <% else %>
        -- <%= t(:ec_extra_hours) %>: <%= totalhours - user.weekly_hours %> h
    <% end %>

<% end %>


Notification sent by El-Capitan plugin